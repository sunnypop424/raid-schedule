# Supabase (Vercel Marketplace 통합을 쓰면 자동 주입). 비워두면 로컬 모드(localStorage)로 동작
NEXT_PUBLIC_SUPABASE_URL=
# publishable 키(sb_publishable_...). 기존 anon 키를 쓰려면 NEXT_PUBLIC_SUPABASE_ANON_KEY 로 넣어도 된다
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Vercel Cron 요청 검증용 임의 문자열 (서버 전용)
CRON_SECRET=
